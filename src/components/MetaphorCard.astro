---
import type { MetaphorWithVotes } from '../lib/types'

interface Props {
  metaphor: MetaphorWithVotes
}

const { metaphor } = Astro.props
---

<article class="border border-gray-200 rounded-lg p-6 hover:border-gray-300 transition-colors">
  <a href={`/metafora/${metaphor.slug}`} class="group block">
    <h2 class="text-2xl font-semibold mb-3 group-hover:text-gray-600 transition-colors">
      {metaphor.nazev}
    </h2>
    <p class="text-gray-700 mb-2">
      {metaphor.definice}
    </p>
    <p class="text-gray-500 italic text-sm mb-2">
      Příklad: {metaphor.priklad}
    </p>
    {metaphor.zdroj && (
      <p class="text-gray-400 text-xs mb-4">
        Zdroj: {metaphor.zdroj}
      </p>
    )}
    <div class="flex items-center gap-4 text-sm text-gray-500">
      <span class="flex items-center gap-1">
        👍 {metaphor.like_count}
      </span>
      <span class="flex items-center gap-1">
        👎 {metaphor.dislike_count}
      </span>
      <span class="ml-auto">
        Skóre: {metaphor.score}
      </span>
    </div>
  </a>
</article>
